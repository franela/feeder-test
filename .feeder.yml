dependencies:
  - crosbymichael/redis

build: &testImage
  - FROM ubuntu
  - RUN apt-get install rlwrap -y wget
  - RUN wget --no-check-certificate https://launchpad.net/~chris-lea/+archive/node.js-devel/+build/5532796/+files/nodejs_0.11.11-1chl1~precise1_amd64.deb; dpkg -i nodejs_0.11.11-1chl1~precise1_amd64.deb
  - WORKDIR /build/
  - CMD ["make","test"]

images:
- feeder/buildImage:%{git.tag}-%{git.branch}-%{git.sha}: *testImage
- feeder/nginx: api/Dockerfile
